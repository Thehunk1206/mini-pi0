"""Task-collector plugin package for ManiSkill dataset collection.

Each plugin module in this package defines one task-aware collector that
implements the `TaskCollector` protocol and registers itself with the
collector registry.
"""
